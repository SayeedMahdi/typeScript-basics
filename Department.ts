class Department{
    constructor(private name:String){}
    }
    function sum2(add1:number, add2:number): number{
        return (add1 + add2);
    }
    function assignObjects <D extends object,T extends object> (a :D, b:T):Array<D,string> 
        const obj= Object.assign(a,b);
    }
 
    













    
    assignObjects(2,3);
    sum2(2,3);





    